<h1>New Book</h1>
<p>本の感想一覧を表示するページ</p>

  <%= form_with model: @book do |f| %>

    <!--本のタイトルを入力するフォーム-->
    <h4>Title</h4>
      <%= f.text_field :title %>

    <!--本の感想を入力するフォーム-->
    <h4>Opinion</h4>
      <%= f.text_area :option %>

    <%= f.submit '投稿' %>
  <% end %>
