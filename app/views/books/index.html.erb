<%= render 'list', book: @book %>

<p>★画面右側に「Books」（投稿された本の一覧）を配置する</p>
<h1>Books</h1>
    <table style="border="1" rules="rows">
    <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
    </thead>

    <tbody>
        <% @books.each do |book| %>
      <tr>
        <td><%= image_tag book.user.get_profile_image(80,80) %></td>
        <td><%= link_to book.title, book_path(book.id) %></td>
        <td><%= book.body %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
